node_modules
dist
coverage
*.log

# Credentials - never commit
.env
.env.*
!.env.example

# Generated image assets are large binaries; keep them out of git history.
# Remove this rule once an asset pipeline (LFS or object storage) is in place.
assets/generated/

.DS_Store
Thumbs.db

# Scratch files from local tooling
_seg*.txt
_prog*.txt
_retry*.txt
_check*.txt

# Vite build output and local dev logs
packages/*/dist/
dev-server*.log

# pnpm workspace
pnpm-lock.yaml keep
